name: Profile stats

# Rebuilds the activity card and contribution snake, then force-pushes them to
# the `output` branch that README.md reads from. Nothing is committed to main.
on:
  schedule:
    - cron: "23 2 * * *" # daily, 07:53 IST
  workflow_dispatch:
  push:
    branches: [main]

permissions:
  contents: write

concurrency:
  group: profile-stats
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7

      - name: Activity card
        run: python3 tools/stats.py --user "${{ github.repository_owner }}" --out dist
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Contribution snake
        uses: Platane/snk@v3
        with:
          github_user_name: ${{ github.repository_owner }}
          github_token: ${{ secrets.GITHUB_TOKEN }}
          outputs: |
            dist/snake-dark.svg?palette=github-dark&color_snake=#ff9d4d&color_dots=#161b22,#4d2c12,#8a4b17,#cc6d1f,#ff9d4d
            dist/snake-light.svg?palette=github-light&color_snake=#e5761f&color_dots=#ebedf0,#fbd9b6,#f6ad6b,#e98a3a,#c1600f

      - name: Publish to the output branch
        working-directory: dist
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          git init -q -b output
          git add -A
          git -c user.name="github-actions[bot]" \
              -c user.email="41898282+github-actions[bot]@users.noreply.github.com" \
              commit -qm "Update profile stats"
          git push -qf "https://x-access-token:${GITHUB_TOKEN}@github.com/${GITHUB_REPOSITORY}.git" output
